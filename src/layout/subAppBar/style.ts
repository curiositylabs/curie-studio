import { BoxProps, Box } from "@mui/material";
import { styled } from "@mui/material/styles";

const SubAppBarWrapper = styled(Box)<BoxProps>({
  width: "100%",
  background: "#D9D9D9",
  padding: "10px 30px"
});

export default SubAppBarWrapper;
